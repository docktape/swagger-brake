# enableScripts is required: vuepress 1.x and fsevents have native
# module postinstall hooks that Yarn Berry disables by default.
enableScripts: true

nodeLinker: node-modules

# Require any installed package to be at least 30 days old.
# Defends against fast-pulled supply-chain attacks.
npmMinimalAgeGate: 30d
